---
import StoryblokComponent from '@storyblok/astro/StoryblokComponent.astro';
import Layout from '../../layouts/Layout.astro';
import { useStoryblokApi } from '@storyblok/astro';

const sbApi = useStoryblokApi();
const { data } = await sbApi.get('cdn/stories/portfolio/webdesign', {
  version: 'draft',
});

const story = data.story;
const bgBlok = story.content.body.find((item) => item.component === 'portfolioHeader');
const seoImg = bgBlok.backgroundImage.filename;
---

<Layout
  title="DSGN. Webdesign Portfolio"
  image={seoImg}
  description="Webdesign Portfolio von Sarah Weihrauch - Freiberufliche Mediendesignerin">
  <StoryblokComponent blok={story.content} />
</Layout>
